name: Build Application and Make Release

on:
  push:
    tags:
      - '*'

jobs:

  build_release:
    name: Build Release
    uses: ./.github/workflows/build.yml
    with:
      build_type: Release
      is_qt_cached: false
    secrets:
      SPARKLE_ED25519_KEY: ${{ secrets.SPARKLE_ED25519_KEY }}
      WINDOWS_CODESIGN_CERT: ${{ secrets.WINDOWS_CODESIGN_CERT }}
      WINDOWS_CODESIGN_PASSWORD: ${{ secrets.WINDOWS_CODESIGN_PASSWORD }}
      CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}

  create_release:
    needs: build_release
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          submodules: 'true'
          path: 'PrismLauncher-source'
      - name: Download artifacts
        uses: actions/download-artifact@v3
      - name: Grab and store version
        run: |
          tag_name=$(echo ${{ github.ref }} | grep -oE "[^/]+$")
          echo "VERSION=$tag_name" >> $GITHUB_ENV
      - name: Package artifacts properly
        run: |
          mv ${{ github.workspace }}/PrismLauncher-source PrismLauncher-${{ github.run_number }}
          mv PrismLauncher-Linux-Qt6-Portable*/PrismLauncher-portable.tar.gz PrismLauncher-Linux-Qt6-Portable-${{ github.run_number }}.tar.gz
          mv PrismLauncher-Linux-Qt6*/PrismLauncher.tar.gz PrismLauncher-Linux-Qt6-${{ github.run_number }}.tar.gz          
          mv PrismLauncher-Linux-Portable*/PrismLauncher-portable.tar.gz PrismLauncher-Linux-Portable-${{ github.run_number }}.tar.gz
          mv PrismLauncher-Linux*/PrismLauncher.tar.gz PrismLauncher-Linux-${{ github.run_number }}.tar.gz
          mv PrismLauncher-*.AppImage/PrismLauncher-*.AppImage PrismLauncher-Linux-${{ github.run_number }}-x86_64.AppImage
          mv PrismLauncher-macOS-Legacy*/PrismLauncher.tar.gz PrismLauncher-macOS-Legacy-${{ github.run_number }}.tar.gz
          mv PrismLauncher-macOS*/PrismLauncher.tar.gz PrismLauncher-macOS-${{ github.run_number }}.tar.gz

          tar -czf PrismLauncher-${{ github.run_number }}.tar.gz PrismLauncher-${{ github.run_number }}

          for d in PrismLauncher-Windows-MSVC*; do
            cd "${d}" || continue
            LEGACY="$(echo -n ${d} | grep -o Legacy || true)"
            ARM64="$(echo -n ${d} | grep -o arm64 || true)"
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="PrismLauncher-Windows-MSVC"
            test -z "${LEGACY}" || NAME="${NAME}-Legacy"
            test -z "${ARM64}" || NAME="${NAME}-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv PrismLauncher-*.exe ../${NAME}-Setup-${{ github.run_number }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ github.run_number }}.zip" *
            cd ..
          done

          for d in PrismLauncher-Windows-MinGW-w64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="PrismLauncher-Windows-MinGW-w64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv PrismLauncher-*.exe ../${NAME}-Setup-${{ github.run_number }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ github.run_number }}.zip" *
            cd ..
          done

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref }}
          release_name: Prism Launcher ${{ github.run_number }}
          draft: false
          prerelease: true
          files: |
            PrismLauncher-Linux-${{ github.run_number }}.tar.gz
            PrismLauncher-Linux-Portable-${{ github.run_number }}.tar.gz
            PrismLauncher-Linux-${{ github.run_number }}-x86_64.AppImage
            PrismLauncher-Linux-Qt6-${{ github.run_number }}.tar.gz
            PrismLauncher-Linux-Qt6-Portable-${{ github.run_number }}.tar.gz
            PrismLauncher-Windows-MinGW-w64-${{ github.run_number }}.zip
            PrismLauncher-Windows-MinGW-w64-Portable-${{ github.run_number }}.zip
            PrismLauncher-Windows-MinGW-w64-Setup-${{ github.run_number }}.exe
            PrismLauncher-Windows-MSVC-Legacy-${{ github.run_number }}.zip
            PrismLauncher-Windows-MSVC-Legacy-Portable-${{ github.run_number }}.zip
            PrismLauncher-Windows-MSVC-Legacy-Setup-${{ github.run_number }}.exe
            PrismLauncher-Windows-MSVC-arm64-${{ github.run_number }}.zip
            PrismLauncher-Windows-MSVC-arm64-Portable-${{ github.run_number }}.zip
            PrismLauncher-Windows-MSVC-arm64-Setup-${{ github.run_number }}.exe
            PrismLauncher-Windows-MSVC-${{ github.run_number }}.zip
            PrismLauncher-Windows-MSVC-Portable-${{ github.run_number }}.zip
            PrismLauncher-Windows-MSVC-Setup-${{ github.run_number }}.exe
            PrismLauncher-macOS-${{ github.run_number }}.tar.gz
            PrismLauncher-macOS-Legacy-${{ github.run_number }}.tar.gz
            PrismLauncher-${{ github.run_number }}.tar.gz
